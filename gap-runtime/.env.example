# Path to the hk 1.57.0 binary installed through mise.
HK_BIN=/path/to/mise/installs/hk/1.57.0/hk
# Optional public-image Docker config if your credential helper is unavailable:
# DOCKER_CONFIG=/path/to/local/docker-config
# Include real Dagger integration in npm test:
# GAP_DAGGER_TEST=1
