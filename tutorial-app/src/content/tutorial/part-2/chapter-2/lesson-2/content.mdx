---
type: lesson
title: Schadenfreude
template: otel-warm-log
focus: /translation.en
prepareCommands:
  - npm install
mainCommand: npm run dev
previews:
  - [4173, "Warm Log"]
terminal: false
editor:
  fileTree: true
custom:
  loanwordArc:
    lessonId: schadenfreude
    translationFile: /translation.en
    vocabularyFile: /PersonalVocabulary.pkl
    runtimeFile: /loanword-runtime.json
    storyFile: /warm-log-story.json
    scenario: schadenfreude-admission
---

import LoanwordArcBridge from '../../../../../components/LoanwordArcBridge';

# Schadenfreude

<LoanwordArcBridge client:load />

Lesson 2 repeats the preservation move and then blocks it.

Start in `translation.en`. A paraphrase still squiggles. If you switch the English text to `Schadenfreude`, the squiggle stays for a new reason: the word is not yet admitted into `PersonalVocabulary.pkl`.

### The Task
1. Edit `translation.en`.
2. Hover the squiggle to inspect the same four structural-loss axes.
3. Type `Schadenfreude` directly and notice that the lesson still stays blocked.
4. Open `PersonalVocabulary.pkl` and add the loanword to `entries`.
5. When validation passes, the preview should say: `you solved this by amending vocabulary, not by refining the gloss`.

### What you are seeing
- `LoanwordRules.pkl` is the authoring source for the structural-loss rule.
- `loanword-runtime.json` is the runtime artifact the browser uses for validation.
- `PersonalVocabulary.pkl` is the admission file for accepted loanwords.
- `warm-log-story.json` is the static warm-log fixture rendered in Monaco.
