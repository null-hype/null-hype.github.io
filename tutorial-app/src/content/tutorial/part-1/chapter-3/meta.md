---
type: chapter
title: CI Evidence & Capability Decisions
---

This chapter is built from real exported evidence, not scripted examples: CIT-147's exporter turns one GitHub Actions run plus its restic snapshots into a browsable lesson (lesson 1), and CIT-147 slice 2 turns capability-spike's real red→approve→green→materialize→reconcile sequence into a model-driven lesson (lesson 2) with a single axiom (`Ledger.pkl`'s `checkAccess()`) visibly connected to the facts it governs, a step control that replays recorded state, and a live in-browser re-evaluation of that axiom -- proven faithful to the real Pkl rule by a conformance test that TutorialKit itself runs, for real, in the lesson's own terminal.
