import { Meta } from "@storybook/addon-docs/blocks";

import EpistemicFrame from "../components/EpistemicFrame";
import InlineFigure from "../components/InlineFigure";
import CaseNode from "../components/CaseNode";
import PullQuote from "../components/PullQuote";
import EvidenceCard from "../components/EvidenceCard";
import AnalystCallout from "../components/AnalystCallout";
import SectionBreak from "../components/SectionBreak";
import ArchiveEntry from "../components/ArchiveEntry";
import WatchlistPanel from "../components/WatchlistPanel";
import { footerLinks, sideRailItems, synthesisContent, topNavItems } from "../data/editorialContent";
import * as EpistemicFrameStories from "./editorial/EpistemicFrame.stories";
import * as InlineFigureStories from "./editorial/InlineFigure.stories";
import * as CaseNodeStories from "./editorial/CaseNode.stories";
import * as PullQuoteStories from "./editorial/PullQuote.stories";
import * as EvidenceCardStories from "./editorial/EvidenceCard.stories";
import * as AnalystCalloutStories from "./editorial/AnalystCallout.stories";
import * as SectionBreakStories from "./editorial/SectionBreak.stories";
import * as ArchiveEntryStories from "./editorial/ArchiveEntry.stories";
import * as WatchlistPanelStories from "./editorial/WatchlistPanel.stories";

export const caseSequence = [
  CaseNodeStories.SignalGrid.args,
  CaseNodeStories.ActionLink.args,
  CaseNodeStories.FactList.args,
  CaseNodeStories.Doctrine.args,
];

export const evidenceCluster = [
  EvidenceCardStories.Metric.args,
  EvidenceCardStories.Quote.args,
  EvidenceCardStories.Signal.args,
];

export const archiveItems = [
  ArchiveEntryStories.Default.args,
  ArchiveEntryStories.Restricted.args,
];

<Meta title="Configure your project" />

<div className="artifact-docs-shell">
  <div className="artifact-docs-intro">
    <p className="artifact-docs-kicker">Editorial Demo Artifact</p>
    <h1 className="artifact-docs-title">jungle.roaring.wave assembled from Storybook stories</h1>
    <p className="artifact-docs-copy">
      This docs page replaces the starter content with a composed longform artifact. The page shell is simple,
      but the substantive reading experience is built from the editorial stories and their args so the same fixtures
      drive both isolated component review and the integrated demo.
    </p>
  </div>

  <div className="editorial-page artifact-demo">
    <div className="editorial-grain" aria-hidden="true"></div>

    <header className="editorial-topbar">
      <div className="editorial-topbar__inner">
        <div className="editorial-topbar__group">
          <span className="editorial-topbar__brand">jungle.roaring.wave</span>
          <nav className="editorial-nav" aria-label="Artifact sections">
            {topNavItems.map((item) => (
              <a href={item.href} className={item.active ? "is-active" : undefined}>
                {item.label}
              </a>
            ))}
          </nav>
        </div>

        <div className="editorial-topbar__actions">
          <button className="editorial-icon-button" type="button" aria-label="Share this artifact">
            <span className="material-symbols-outlined" aria-hidden="true">share</span>
          </button>
          <button className="editorial-icon-button" type="button" aria-label="Bookmark this artifact">
            <span className="material-symbols-outlined" aria-hidden="true">bookmark</span>
          </button>
          <a className="editorial-pdf-link" href="#artifact-footer">Dossier PDF</a>
        </div>
      </div>
    </header>

    <aside className="editorial-sidebar" aria-label="Dossier shortcuts">
      <div className="editorial-sidebar__inner">
        <div className="editorial-sidebar__brand">
          <span className="editorial-sidebar__brand-mark">AIU</span>
          <span className="editorial-sidebar__ref">Ref: JRW-24</span>
        </div>

        <nav className="editorial-sidebar__nav" aria-label="Section shortcuts">
          {sideRailItems.map((item) => (
            <a href={item.href} className={item.active ? "is-active" : undefined}>
              <span className="editorial-sidebar__icon material-symbols-outlined" aria-hidden="true">
                {item.icon}
              </span>
              <span>{item.label}</span>
            </a>
          ))}
        </nav>
      </div>
    </aside>

    <main className="editorial-main">
      <section id="epistemic" className="editorial-section editorial-section--bordered">
        <div className="editorial-container">
          <EpistemicFrame {...EpistemicFrameStories.Default.args} />
        </div>
      </section>

      <div className="editorial-block">
        <div className="editorial-container">
          <InlineFigure {...InlineFigureStories.Default.args} />
        </div>
      </div>

      <section id="cases" className="editorial-section">
        <div className="editorial-container">
          <header className="editorial-section-heading">
            <h2 className="editorial-section-heading__title">Case Sequence</h2>
            <p className="editorial-section-heading__eyebrow">A chronological analysis of actor-driven disruptions.</p>
          </header>

          <div className="editorial-case-stack">
            <CaseNode {...caseSequence[0]} />
            <PullQuote {...PullQuoteStories.Default.args} />
            <CaseNode {...caseSequence[1]} />
            <div className="evidence-grid">
              {evidenceCluster.map((card) => (
                <EvidenceCard {...card} />
              ))}
            </div>
            <CaseNode {...caseSequence[2]} />
            <AnalystCallout {...AnalystCalloutStories.Accent.args} />
            <CaseNode {...caseSequence[3]} />
          </div>
        </div>
      </section>

      <div id="deep-archives" className="editorial-section-break-wrap">
        <div className="editorial-container">
          <SectionBreak {...SectionBreakStories.Default.args} />
        </div>
      </div>

      <section className="editorial-section archive-section">
        <div className="editorial-container archive-section__grid">
          <div className="archive-section__intro">
            <h2 className="archive-section__title">Historical Analogues</h2>
            <p className="archive-section__eyebrow">Comparative Signal Analysis.</p>
          </div>
          <div className="archive-section__list">
            {archiveItems.map((entry) => (
              <ArchiveEntry {...entry} />
            ))}
          </div>
        </div>
      </section>

      <section id="synthesis" className="editorial-section">
        <div className="editorial-container synthesis-section__grid">
          <div className="synthesis-section__copy">
            <h2 className="synthesis-section__title">{synthesisContent.title}</h2>
            <div className="synthesis-copy">
              <p>{synthesisContent.paragraphs[0]}</p>
              <p className="is-muted">{synthesisContent.paragraphs[1]}</p>
            </div>
          </div>
          <div className="synthesis-section__panel">
            <WatchlistPanel {...WatchlistPanelStories.Default.args} />
          </div>
        </div>
      </section>
    </main>

    <footer className="editorial-footer" id="artifact-footer">
      <div className="editorial-container editorial-footer__grid">
        <div className="editorial-footer__copy">
          <span className="editorial-footer__brand">jungle.roaring.wave</span>
          <p className="editorial-footer__meta">Demo artifact sourced from Storybook fixtures.</p>
          <p className="editorial-footer__legal">
            Each major section above is composed from the same args used by the editorial component stories.
          </p>
        </div>

        <div className="editorial-footer__links">
          {footerLinks.map((link) => (
            <a href={link.href}>{link.label}</a>
          ))}
        </div>
      </div>
    </footer>
  </div>
</div>

<style>
  {`
    .artifact-docs-shell {
      margin: -1rem;
      background: #f6f3ee;
    }

    .artifact-docs-intro {
      padding: 2rem 2rem 1rem;
      border-bottom: 1px solid rgba(26, 26, 27, 0.1);
      background: #fcf9f4;
    }

    .artifact-docs-kicker {
      margin: 0 0 0.75rem;
      font-family: var(--font-label);
      font-size: 0.72rem;
      font-weight: 800;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--editorial-accent);
    }

    .artifact-docs-title {
      margin: 0;
      font-family: var(--font-display);
      font-size: clamp(2rem, 4vw, 3rem);
      font-weight: 800;
      letter-spacing: -0.05em;
      line-height: 0.95;
      color: var(--editorial-ink);
    }

    .artifact-docs-copy {
      max-width: 44rem;
      margin: 1rem 0 0;
      font-family: var(--font-display);
      font-size: 1.05rem;
      line-height: 1.6;
      color: rgba(26, 26, 27, 0.78);
    }

    .artifact-demo .editorial-topbar {
      position: sticky;
    }

    .artifact-demo .editorial-sidebar {
      position: sticky;
      display: none;
      top: 4rem;
      height: auto;
      align-self: start;
    }

    .artifact-demo .editorial-main {
      padding-top: 0;
      padding-left: 0;
    }

    .artifact-demo .editorial-grain {
      position: absolute;
    }

    @media (min-width: 64rem) {
      .artifact-demo .editorial-sidebar {
        display: block;
      }

      .artifact-demo .editorial-main {
        padding-left: var(--sidebar-width);
      }
    }
  `}
</style>
